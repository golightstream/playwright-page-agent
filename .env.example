# Copy to .env and fill in (never commit .env).
# https://console.anthropic.com/ (Claude), https://platform.openai.com/ (GPT), etc.

AGENT_API_KEY=
# Optional overrides:
# AGENT_BASE_URL=
# AGENT_MODEL=claude-haiku-4-5-20251001
